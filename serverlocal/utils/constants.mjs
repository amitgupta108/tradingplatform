import config from '../config/scrips_config.json' with { type: 'json' };

export const {
    OPT_EXPIRIES,
    FUT_EXPIRIES,
    OPT_CONFIG,
    EXCHANGES,
    STRIKE_SIZE,
    filePaths,
    filters,
    tp_filters
} = config;